apiVersion: groot.koronet.com/v1alpha1
kind: GrootApp
metadata:
    annotations:
        groot.koronet.com/description: TX Portfolio — Chapter TX wholesaler dashboard
        groot.koronet.com/labs-template: static-site
    labels:
        groot.koronet.com/lane: labs
        groot.koronet.com/product: labs
        groot.koronet.com/tier: tier-4-low
    name: tx-portfolio
spec:
    cells:
        - default: true
          deployments:
            - workload: api
          environment: production
          name: labs
          routes:
            - access:
                policyRef: tx-portfolio-labs
                provider: cloudflare-access
              host: tx-portfolio.koronetlabs.com
              name: web
              port: http
              visibility: edge-protected
              workload: api
          target: koronet-labs-us-east-1
    workloads:
        - name: api
          type: api
